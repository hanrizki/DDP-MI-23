#include <iostream>
using namespace std;

int main()
{
    int u, r;
    cin >> u >> r;
    if (u <= r)
    {
        for (int i = u; i <= r; i++)
        {
            cout << i << " ";
        }
    }
    else
    {
        for (int i = u; i <= r; i++)
        {
            cout << i << " ";
        }
    }
    cout << endl;
    return 0;
}